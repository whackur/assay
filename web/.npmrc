# Next.js standalone output and several Next plugins expect a hoisted
# node_modules layout (node_modules/<pkg> rather than pnpm's isolated store
# symlinks). Keep node-linker=hoisted so `next build` and `eslint .` resolve
# binaries and peer dependencies the same way npm's flat layout did.
node-linker=hoisted